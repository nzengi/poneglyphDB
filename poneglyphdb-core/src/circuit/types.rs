//! Circuit types

/// Wire identifier
pub type Wire = usize;

/// Field element type (placeholder - will use actual field type from halo2)
pub type Field = u64;

/// Gate identifier
pub type GateId = usize;

/// Constraint identifier
pub type ConstraintId = usize;

